---
name: grill-me
description: Run a grilling session to stress-test a plan or design before building. User-invoked orchestrator over the grilling skill.
disable-model-invocation: true
---

Run a `/grilling` session — read and follow `.claude/skills/grilling/SKILL.md`.

For **upstream sync** runs (merging simstudioai/sim into version-4.2-main), use `/upstream-sync-grill` instead — it adds ledger-aware, PR-based Q&A rules for automated harness runs.
