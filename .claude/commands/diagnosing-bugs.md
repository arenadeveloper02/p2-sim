---
description: Diagnose a failing test, build error, or merge regression using the structured diagnosis loop
argument-hint: [symptom or failing command]
---

Read and execute `.claude/skills/diagnosing-bugs/SKILL.md` in full.

User context: $ARGUMENTS

For upstream sync runs, start from the failing verification command logged in `.upstream-sync/ledger/*/run.md`.
