/** OAuth / credential `providerId` for LinkedIn via Unipile (safe for client components). */
export const UNIPILE_LINKEDIN_PROVIDER_ID = 'unipile_linkedin' as const

export const UNIPILE_HOSTED_SUCCESS_PARAM = 'unipile_hosted' as const
export const UNIPILE_HOSTED_ACCOUNT_ID_PARAM = 'account_id' as const
